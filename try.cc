#include <iostream>
#include <vector>
#include <algorithm>
#include <utility>
#include <cmath>
#define SPEEDUP ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
using namespace std;
//15992
/*
정수 n과 m(0 < m < n <1000)이 주어졌을 때, m개의 수를 사용하여 n을 1, 2, 3의 합으로 나타내는 방법의 수를 구하라.
*/

int T, N, M;
vector<int> answer;

int main(void){
    SPEEDUP
    cin >> T;
    for(int i = 0; i < T; i++){
        int ans;
        cin >> N >> M;
        answer.push_back(ans);

    }
}
